import styled from 'styled-components';

export const Container = styled.div`
  .themed {
    border-radius: 12px;
    box-shadow: 0 0 0.68rem rgba(0, 0, 0, 0.1);

    &.clickable {
      padding-right: 40px;
    }
  }
`;
